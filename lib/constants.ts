import { Car, Eye, Clock, Scan, DivideIcon as LucideIcon } from 'lucide-react';

export const templateIcons: Record<string, LucideIcon> = {
  'guess-year-model': Car,
  'odometer-challenge': Eye,
  'fastest-finger': Clock,
  'badge-guess': Scan,
};
